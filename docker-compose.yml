version: "3.8"

services:
  whatsapp-seerr:
    image: ghcr.io/mercuryvapors/whatsapp-seerr:latest
    container_name: whatsapp-seerr
    restart: unless-stopped
    ports:
      - "7000:7000"
    # Chromium's renderer crashes with Docker's default 64MB /dev/shm
    shm_size: 1gb
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
      - PUPPETEER_SKIP_DOWNLOAD=false
      - PORT=7000